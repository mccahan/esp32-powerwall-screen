name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Required for GitHub Pages deployment
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Cache pip
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/platformio.ini') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Cache PlatformIO
      uses: actions/cache@v4
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-platformio-${{ hashFiles('**/platformio.ini') }}
        restore-keys: |
          ${{ runner.os }}-platformio-
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install PlatformIO
      run: |
        python -m pip install --upgrade pip
        pip install platformio
    
    - name: Install dependencies for SVG conversion
      run: |
        pip install cairosvg pillow
    
    - name: Generate UI assets from SVG files
      run: |
        python3 tools/convert_svg_to_lvgl.py assets/layout.svg src/ui_assets/layout_img.c layout_img 480 480
        python3 tools/convert_svg_to_lvgl.py assets/grid_offline.svg src/ui_assets/grid_offline_img.c grid_offline_img 79 81
    
    - name: Build firmware
      run: pio run
    
    - name: Copy firmware files
      run: |
        mkdir -p dist
        cp .pio/build/esp32-s3-devkitc-1/firmware.bin dist/
        cp .pio/build/esp32-s3-devkitc-1/bootloader.bin dist/ || echo "Bootloader not found"
        cp .pio/build/esp32-s3-devkitc-1/partitions.bin dist/ || echo "Partitions not found"
    
    - name: Upload firmware artifacts
      uses: actions/upload-artifact@v4
      with:
        name: firmware
        path: dist/
        retention-days: 30
    
    - name: Prepare GitHub Pages
      if: github.ref == 'refs/heads/main'
      run: |
        mkdir -p public
        cp dist/*.bin public/
        # Generate version for cache busting
        VERSION=$(date +%Y%m%d.%H%M%S)
        echo "MANIFEST_VERSION=${VERSION}" >> $GITHUB_ENV
    
    - name: Create manifest.json with versioning
      if: github.ref == 'refs/heads/main'
      run: |
        # Create versioned manifest (using unquoted EOF to allow variable expansion)
        cat > public/manifest-${MANIFEST_VERSION}.json << EOF
        {
          "name": "ESP32 Powerwall Display",
          "version": "${MANIFEST_VERSION}",
          "home_assistant_domain": "esphome",
          "funding_url": "https://github.com/mccahan/esp32-powerwall-screen",
          "new_install_prompt_erase": true,
          "builds": [
            {
              "chipFamily": "ESP32-S3",
              "parts": [
                {
                  "path": "bootloader.bin",
                  "offset": 0
                },
                {
                  "path": "partitions.bin",
                  "offset": 32768
                },
                {
                  "path": "firmware.bin",
                  "offset": 65536
                }
              ]
            }
          ]
        }
        EOF
        
        # Create a latest manifest symlink/copy for backwards compatibility
        cp public/manifest-${MANIFEST_VERSION}.json public/manifest.json
    
    - name: Create ESP Web Tools page
      if: github.ref == 'refs/heads/main'
      run: |
        # Create index.html (using unquoted EOF to allow variable expansion)
        cat > public/index.html << EOF
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
            <meta http-equiv="Pragma" content="no-cache">
            <meta http-equiv="Expires" content="0">
            <title>ESP32 Powerwall Display - Web Installer</title>
            <script type="module" src="https://unpkg.com/esp-web-tools@10.0.1/dist/web/install-button.js?module"></script>
            <style>
                body {
                    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
                    max-width: 800px;
                    margin: 0 auto;
                    padding: 20px;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    min-height: 100vh;
                }
                .container {
                    background: white;
                    border-radius: 12px;
                    padding: 40px;
                    box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                }
                h1 {
                    color: #333;
                    margin-bottom: 10px;
                }
                .subtitle {
                    color: #666;
                    margin-bottom: 30px;
                }
                .version {
                    color: #999;
                    font-size: 0.85em;
                    margin-top: 5px;
                }
                esp-web-install-button {
                    margin: 20px 0;
                }
                .info {
                    background: #f5f5f5;
                    padding: 20px;
                    border-radius: 8px;
                    margin-top: 30px;
                }
                .info h2 {
                    margin-top: 0;
                    color: #333;
                }
                .info ul {
                    color: #666;
                    line-height: 1.8;
                }
                .warning {
                    background: #fff3cd;
                    border: 1px solid #ffc107;
                    padding: 15px;
                    border-radius: 8px;
                    margin-top: 20px;
                    color: #856404;
                }
            </style>
        </head>
        <body>
            <div class="container">
                <h1>🔋 ESP32 Powerwall Display</h1>
                <p class="subtitle">Web-based firmware installer for your ESP32-S3 device</p>
                <p class="version">Version: ${MANIFEST_VERSION}</p>
                
                <esp-web-install-button manifest="manifest.json?v=${MANIFEST_VERSION}">
                    <button slot="activate">Install Firmware</button>
                    <span slot="unsupported">
                        Your browser doesn't support Web Serial. Try Chrome or Edge.
                    </span>
                    <span slot="not-allowed">
                        Not allowed to use Web Serial. Check your browser permissions.
                    </span>
                </esp-web-install-button>
                
                <div class="warning">
                    ⚠️ <strong>Important:</strong> This installer uses Web Serial API and requires Chrome, Edge, or Opera browser.
                </div>
                
                <div class="info">
                    <h2>Features</h2>
                    <ul>
                        <li>🎨 LVGL-based 480x480 display interface</li>
                        <li>📡 Improv WiFi for easy credential configuration</li>
                        <li>🔌 Browser-based firmware installation (no tools needed)</li>
                        <li>💾 Support for ESP32-S3 devices with 16MB flash</li>
                        <li>⚡ Optimized for Guition ESP32-S3-4848S040</li>
                    </ul>
                </div>
                
                <div class="info">
                    <h2>Instructions</h2>
                    <ul>
                        <li>Connect your ESP32-S3 device via USB</li>
                        <li>Click "Install Firmware" button above</li>
                        <li>Select your device's serial port</li>
                        <li>Wait for the installation to complete</li>
                        <li>Configure WiFi using Improv protocol</li>
                        <li>View "Hello World" on your 480x480 display!</li>
                    </ul>
                </div>
            </div>
        </body>
        </html>
        EOF
    
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public
        force_orphan: true
