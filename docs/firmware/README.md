# Firmware Files

This directory will contain the compiled firmware files after the GitHub Actions build process.

Files that will be generated:
- `bootloader.bin` - ESP32-S3 bootloader
- `partitions.bin` - Partition table
- `firmware.bin` - Main application firmware

These files are used by the ESP Web Tools installer at https://mccahan.github.io/esp32-powerwall-screen/
